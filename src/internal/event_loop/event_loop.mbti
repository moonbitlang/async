package "moonbitlang/async/internal/event_loop"

// Values

// Types and methods
type EventLoop
fn EventLoop::close(Self, Int) -> Unit
fn EventLoop::new() -> Self
fn EventLoop::run_forever(Self) -> Unit raise
async fn EventLoop::sleep(Self, Int) -> Unit raise
async fn EventLoop::wait_for_fd_read(Self, Int) -> Unit raise
async fn EventLoop::wait_for_fd_write(Self, Int) -> Unit raise

pub(all) enum Events {
  Read
  Write
  ReadWrite
  Error
}

// Type aliases

// Traits

