name: check

on:
  push:
    branches:
      - main
  pull_request:

env:
  MOONBIT_ASYNC_CHECK_FD_LEAK: 1

jobs:
  pidfd-sigkill-line-limit-repro:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      MOONBIT_NEW_NATIVE: 0
      MOONBIT_ALLOCATOR: system
    steps:
      - uses: actions/checkout@v4

      - name: build repro
        run: |
          gcc -O0 -Wall -Wextra -fsanitize=address tools/pidfd_sigkill_line_limit_repro.c -o /tmp/pidfd_sigkill_line_limit_repro
          gcc -O0 -g -fno-omit-frame-pointer -fsanitize=address -Wall -Wextra tools/pidfd_repro_cat.c -o /tmp/pidfd_repro_cat_asan

      - name: run repro
        run: |
          /tmp/pidfd_sigkill_line_limit_repro 10000 /tmp/pidfd_repro_cat_asan 1000 20
          ASAN_OPTIONS=detect_leaks=0 /tmp/pidfd_sigkill_line_limit_repro 10000 /tmp/pidfd_repro_cat_asan 1000 20

  sanitizer-check:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      MOONBIT_NEW_NATIVE: 0
      MOONBIT_ALLOCATOR: system
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v6
        with:
          node-version: '>=22.4.0'

      - name: install
        run: |
          curl -fsSL https://cli.moonbitlang.com/install/unix.sh | bash
          echo "$HOME/.moon/bin" >> $GITHUB_PATH

      - name: moon version
        run: |
          moon version --all
          moonrun --version

      - name: moon update
        run: |
          moon update

      - name: setup sanitizer check
        run: |
          moon run --debug test_programs/setup_sanitizer_check
          gcc -O0 -g -fno-omit-frame-pointer -fsanitize=address -Wall -Wextra tools/pidfd_repro_cat.c -o /tmp/pidfd_repro_cat_asan

      - name: run test with address sanitizer
        run: |
          export MOONBIT_ASYNC_TEST_SANITIZER_ENABLED=1
          export MOONBIT_ASYNC_C_CAT_PROG=/tmp/pidfd_repro_cat_asan
          for filter in \
            "min process c cat drained stdout writer closes" \
            "min process c cat pending stdout writer closes" \
            "min process c cat drained stdout writer held open" \
            "min process c cat pending stdout writer held open" \
            "min process c cat stdout pipe unread writer held open" \
            "min process c cat no stdout pipe writer held open" \
            "min process sleep no pipes synthetic cancellation"; do
            echo "======================== ${filter} ============================="
            for _ in $(seq 1 20); do
              moon test src/shell/line_cap_minimize_test.mbt -F "$filter"
            done
          done
