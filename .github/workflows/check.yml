name: check

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  stable-build:
    strategy:
      fail-fast: false
      matrix:
        os: ["windows-latest"]

    runs-on: ${{ matrix.os }}
    continue-on-error: false
    timeout-minutes: 10
    steps:
      - name: disable auto CRLF
        if: ${{ matrix.os == 'windows-latest' }}
        run: git config --global core.autocrlf false

      - uses: actions/checkout@v4
      - uses: ilammy/msvc-dev-cmd@v1

      - name: install (Windows)
        if: ${{ matrix.os == 'windows-latest' }}
        run: |
          Set-ExecutionPolicy RemoteSigned -Scope CurrentUser; irm https://cli.moonbitlang.com/install/powershell.ps1 | iex
          "C:\Users\runneradmin\.moon\bin" | Out-File -FilePath $env:GITHUB_PATH -Append

      - name: 0
        run: moon test src/socket/dual_stack_test.mbt --index 0 --release

      - name: 1
        run: moon test src/socket/dual_stack_test.mbt --index 1 --release

      - name: 2
        run: moon test src/socket/dual_stack_test.mbt --index 2 --release

      - name: 3
        run: moon test src/socket/dual_stack_test.mbt --index 3 --release

      - name: 4
        run: moon test src/socket/dual_stack_test.mbt --index 4 --release

      - name: 5
        run: moon test src/socket/dual_stack_test.mbt --index 5 --release
